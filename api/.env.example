APP_NAME=sprintOS
APP_ENV=local
APP_KEY=
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_DATABASE=sprintos
QUEUE_CONNECTION=sqs
# LLM providers are configured per-deployment in the DB (encrypted), not here.
