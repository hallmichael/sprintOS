/**
 * Local FileManager build. Source lives in components/fileManagerSource/.
 * After editing source, run: yarn build:file-manager
 */
import './dist/style.css';
export { FileManager } from './dist/react-file-manager.es.js';
